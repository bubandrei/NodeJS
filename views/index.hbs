<!DOCTYPE html>
<html>

<head>
	<title>{{ title }}</title>
</head>

<body>
	<header>
		{{!-- {{> header/menu}} --}}
	</header>
	<main>
		<div>index</div>
		{{!-- <div>{{#with product}}
			Object {{name}} {{cost}} {{amount}}
			<div>{{../amount}}</div>
			{{/with}}
		</div> --}}
		<ul>
			{{#each product}}
			<li>{{.}}</li>
			{{/each}}
		</ul>
	</main>
	<footer>
		{{!-- {{> footer/contact}} --}}
	</footer>
</body>

</html>